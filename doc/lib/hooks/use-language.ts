'use client'

/** Re-export of the language hook so consumers can import from @/lib/hooks. */
export { useLanguage } from '@/lib/language-context'
export type { Language } from '@/lib/language-context'
